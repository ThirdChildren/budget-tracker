import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.stephl0xff.budgettracker',
  appName: 'Budget Tracker',
  webDir: 'dist'
};

export default config;
